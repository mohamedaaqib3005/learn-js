// Type Aliases

type User = {
  name: String,
  age: Number,
  married: Boolean
}

const Baddi: User = {
  name: "Yash",
  age: 27,
  married: true,
}

const George: User = {
  name: "Pauly",
  age: thirty - one,// error cannot find the name thirty and one
  married: no,// error cannot find the name no
}